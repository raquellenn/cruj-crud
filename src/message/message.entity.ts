import { User } from '../users/user.entity';

export class Message {
  // sender: User;
  // receiver: User;
  // content: string;
  // createdAt: Date;

  // static sortMessages(message: Message) {
  //   return message;
  // }
}
